/*const { sum } = require("../components/sum");

const assert = require("chai").assert;
const expect = require("chai").expect;
const should = require("chai").should();

describe("#sum()", () => {
  context("without arguments", () => {
    it("should return 0", () => {
      expect(sum()).to.equal(0);
    });
  });

  context("with only one argument", () => {
    it("should return 0", () => {
      expect(sum(3)).to.equal(0);
    });
  });

  context("with argumnets", () => {
    it("should return sum of arguments", () => {
      expect(sum(3, 5)).to.equal(15);
    });
  });
});
*/
